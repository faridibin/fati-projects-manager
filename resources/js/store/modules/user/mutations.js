export default {
    SET_USER: (state, user) => {
        state.user = user
    },
    SET_SETTINGS: (state, settings) => {
        state.settings = settings
    },
    SET_AVATAR: (state, avatar) => {
        state.avatar = avatar
    }
}
